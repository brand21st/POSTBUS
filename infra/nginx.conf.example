upstream postbus_next {
  server 127.0.0.1:3000;
}

# PostBus only. Never add vachat.in or www.vachat.in — that site is a separate project.
server {
  listen 80;
  server_name postbus.vachat.in www.postbus.vachat.in *.postbus.vachat.in postbus.in www.postbus.in *.postbus.in;
  client_max_body_size 20m;

  location / {
    proxy_pass http://postbus_next;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}
